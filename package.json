{
  "name": "denzel-react-portfolio",
  "homepage": "https://denzelhartshorn.github.com/deploy-react-portfolio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "browserlist": "^1.0.1",
    "browserslist": "^4.7.0",
    "caniuse-lite": "^1.0.30000998",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-scripts": "2.1.1"
  },
  "engines": {
    "node": ">=10.0.0",
    "npm": ">=6.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "herokupostbuild": "npm install --prefix client && npm run build --prefix client"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "resume": "file:React-Portfolio"
  }
}
